# Minimal requirements for running the pose-transfer pipeline
# Versions are recommendations; update as needed for your environment
# NOTE: mediapipe 1.0+ removed the mp.solutions API used by this project.
# Pin to a 0.10.x release that retains the solutions API (0.10.14 is tested).
torch
diffusers[torch]
transformers
accelerate
safetensors
opencv-python
Pillow
mediapipe==0.10.14
numpy
scipy
gradio
